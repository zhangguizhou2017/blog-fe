# RSC 工作原理可视化对比

## 1. Server Component vs Client Component

### Server Component（服务器组件）

```
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器                            │
│                                                         │
│  访问 http://localhost:3000                            │
│                                                         │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP 请求
                     ↓
┌─────────────────────────────────────────────────────────┐
│                  Next.js 服务器                         │
│                                                         │
│  1. 接收请求                                           │
│  2. 执行 app/page.tsx (Server Component)              │
│  3. 执行 getPosts() 函数                              │
│  4. 创建 Supabase 客户端                              │
│  5. 直接查询数据库                                     │
│     SELECT * FROM posts WHERE published=true          │
│  6. 获取作者信息                                       │
│     SELECT * FROM user_profiles WHERE id IN (...)    │
│  7. 合并数据                                           │
│  8. React 在服务器上渲染组件                          │
│  9. 生成完整的 HTML                                    │
│                                                         │
└────────────────────┬────────────────────────────────────┘
                     │ 返回完整的 HTML（包含数据）
                     ↓
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器                            │
│                                                         │
│  接收 HTML                                             │
│  直接显示（不需要加载状态）                            │
│                                                         │
│  ✅ 首屏快                                             │
│  ✅ SEO 友好                                           │
│  ✅ 不需要 JavaScript 处理数据                         │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Client Component（客户端组件）

```
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器                            │
│                                                         │
│  访问 http://localhost:3000/blog/123                  │
│                                                         │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP 请求
                     ↓
┌─────────────────────────────────────────────────────────┐
│                  Next.js 服务器                         │
│                                                         │
│  1. 接收请求                                           │
│  2. 返回包含 React 组件的 HTML                        │
│                                                         │
└────────────────────┬────────────────────────────────────┘
                     │ 返回 HTML + JavaScript
                     ↓
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器                            │
│                                                         │
│  1. 接收 HTML                                          │
│  2. 加载 JavaScript                                    │
│  3. React 在浏览器上挂载组件                          │
│  4. useEffect 触发                                     │
│  5. 发送 HTTP 请求获取数据                            │
│                                                         │
│  显示加载中...                                         │
│                                                         │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP 请求 GET /api/comments?postId=123
                     ↓
┌─────────────────────────────────────────────────────────┐
│                  Next.js 服务器                         │
│                                                         │
│  1. 接收 API 请求                                      │
│  2. 执行 app/api/comments/route.ts                    │
│  3. 查询数据库                                         │
│     SELECT * FROM comments WHERE post_id=123         │
│  4. 返回 JSON 数据                                     │
│                                                         │
└────────────────────┬────────────────────────────────────┘
                     │ 返回 JSON
                     ↓
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器                            │
│                                                         │
│  1. 接收 JSON                                          │
│  2. 更新状态 setComments(data)                        │
│  3. 重新渲染组件                                       │
│  4. 显示评论列表                                       │
│                                                         │
│  ❌ 首屏慢                                             │
│  ❌ 需要加载状态                                       │
│  ✅ 可以处理用户交互                                   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 四种数据获取方式对比

### 方式 1：Server Component（最优）

```
用户请求
   ↓
服务器执行 async 函数
   ↓
直接查询数据库
   ↓
返回完整 HTML
   ↓
浏览器显示

⏱️  时间：最快
📡 请求数：1 次
📦 JS 体积：最小
```

### 方式 2：Server Action（推荐）

```
用户交互（点击按钮）
   ↓
调用 Server Action
   ↓
服务器执行函数
   ↓
直接查询数据库
   ↓
返回结果
   ↓
浏览器更新 UI

⏱️  时间：快
📡 请求数：1 次
📦 JS 体积：小
```

### 方式 3：Client Component + useEffect（常用）

```
页面加载
   ↓
React 挂载组件
   ↓
useEffect 触发
   ↓
浏览器发送 HTTP 请求
   ↓
服务器查询数据库
   ↓
返回 JSON
   ↓
浏览器更新状态
   ↓
重新渲染

⏱️  时间：中等
📡 请求数：2 次
📦 JS 体积：中等
```

### 方式 4：API 路由（第三方）

```
第三方应用发送 HTTP 请求
   ↓
服务器接收 API 请求
   ↓
查询数据库
   ↓
返回 JSON
   ↓
第三方应用处理数据

⏱️  时间：中等
📡 请求数：1 次
📦 JS 体积：无关
```

---

## 3. 你的项目中的实际应用

### 首页 (app/page.tsx)

```
用户访问 /
   ↓
Server Component 执行
   ↓
getPosts() 函数
   ↓
直接查询数据库
   ↓
获取文章列表
   ↓
获取作者信息
   ↓
合并数据
   ↓
返回完整 HTML
   ↓
浏览器显示文章列表

✅ 首屏快
✅ SEO 友好
✅ 不需要加载状态
```

### 评论区 (components/comments-section.tsx)

```
页面加载
   ↓
CommentsSection 组件挂载
   ↓
useEffect 触发
   ↓
浏览器发送请求
   ↓
服务器查询数据库
   ↓
返回评论列表
   ↓
更新状态
   ↓
显示评论

✅ 可以处理用户交互
✅ 可以实时更新
❌ 首屏需要加载
```

### 分类管理 (app/categories/manage/page.tsx)

```
用户访问 /categories/manage
   ↓
Client Component 挂载
   ↓
useEffect 获取分类列表
   ↓
用户填写表单
   ↓
用户点击"创建"
   ↓
调用 Supabase 客户端
   ↓
插入数据库
   ↓
更新本地状态
   ↓
显示新分类

✅ 完整的表单交互
✅ 实时反馈
```

### API 路由 (app/api/categories/route.ts)

```
第三方应用
   ↓
GET /api/categories
   ↓
服务器接收
   ↓
查询数据库
   ↓
返回 JSON
   ↓
第三方应用处理

✅ 支持第三方调用
✅ 支持 CORS
✅ 支持移动应用
```

---

## 4. 性能对比

### 首屏加载时间

```
Server Component    ████████████████████ 最快 (100ms)
Server Action       ████████████████████ 最快 (100ms)
Client Component    ████████████░░░░░░░░ 中等 (300ms)
API 路由            ████████████░░░░░░░░ 中等 (300ms)
传统前后端分离      ████░░░░░░░░░░░░░░░░ 较慢 (500ms)
```

### 网络请求数

```
Server Component    █ 1 次
Server Action       █ 1 次
Client Component    ██ 2 次
API 路由            █ 1 次
传统前后端分离      ███ 3+ 次
```

### JavaScript 体积

```
Server Component    █ 最小
Server Action       ██ 小
Client Component    ███ 中等
API 路由            ███ 中等
传统前后端分离      █████ 最大
```

---

## 5. 决策流程图

```
需要获取数据？
│
├─ 否
│  │
│  ├─ 需要使用 hooks？
│  │  ├─ 是 → Client Component
│  │  └─ 否 → Server Component
│  │
│  └─ 需要处理用户交互？
│     ├─ 是 → Client Component
│     └─ 否 → Server Component
│
└─ 是
   │
   ├─ 需要在初始加载时获取？
   │  ├─ 是 → Server Component
   │  └─ 否 → Client Component + useEffect
   │
   ├─ 需要用户交互触发？
   │  ├─ 是 → Server Action 或 API 路由
   │  └─ 否 → Server Component
   │
   └─ 需要第三方应用调用？
      ├─ 是 → API 路由
      └─ 否 → Server Component 或 Server Action
```

---

## 6. 代码示例对比

### 获取分类列表

#### Server Component（推荐）

```typescript
async function getCategories() {
  const supabase = createServerClient()
  const { data } = await supabase.from("categories").select("*")
  return data
}

export default async function CategoriesPage() {
  const categories = await getCategories()
  return <div>{categories.map(...)}</div>
}
```

#### Client Component

```typescript
"use client"

export default function CategoriesPage() {
  const [categories, setCategories] = useState([])

  useEffect(() => {
    const fetchCategories = async () => {
      const { data } = await supabase.from("categories").select("*")
      setCategories(data)
    }
    fetchCategories()
  }, [])

  return <div>{categories.map(...)}</div>
}
```

#### API 路由

```typescript
// app/api/categories/route.ts
export async function GET() {
  const supabase = createServerClient();
  const { data } = await supabase.from("categories").select("*");
  return NextResponse.json({ data });
}

// 客户端调用
const response = await fetch("/api/categories");
const { data } = await response.json();
```

---

## 7. 总结表

| 特性         | Server Component | Client Component | Server Action | API 路由  |
| ------------ | ---------------- | ---------------- | ------------- | --------- |
| 执行位置     | 服务器           | 浏览器           | 服务器        | 服务器    |
| 何时执行     | 页面加载         | 组件挂载         | 用户交互      | HTTP 请求 |
| 首屏速度     | ⚡⚡⚡ 最快      | ⚡⚡ 中等        | ⚡⚡⚡ 最快   | ⚡⚡ 中等 |
| 网络请求     | 1 次             | 2 次             | 1 次          | 1 次      |
| 可访问数据库 | ✅               | ❌               | ✅            | ✅        |
| 可使用 hooks | ❌               | ✅               | ❌            | ❌        |
| 可处理交互   | ❌               | ✅               | ✅            | ❌        |
| 第三方调用   | ❌               | ❌               | ❌            | ✅        |
| 代码复杂度   | 低               | 中               | 低            | 中        |
| 推荐使用     | 初始数据         | 交互式 UI        | 表单提交      | 第三方    |

---

## 8. 你的项目中的最佳实践

```
首页 (app/page.tsx)
├─ 类型：Server Component ✅
├─ 原因：初始数据加载
└─ 优势：首屏快，SEO 友好

文章详情 (app/blog/[slug]/page.tsx)
├─ 类型：Server Component ✅
├─ 原因：初始数据加载
└─ 优势：首屏快，SEO 友好

评论区 (components/comments-section.tsx)
├─ 类型：Client Component ✅
├─ 原因：需要实时交互
└─ 优势：可以处理用户交互

分类管理 (app/categories/manage/page.tsx)
├─ 类型：Client Component ✅
├─ 原因：需要表单交互
└─ 优势：完整的表单功能

API 路由 (app/api/categories/route.ts)
├─ 类型：API 路由 ✅
├─ 原因：第三方应用调用
└─ 优势：支持 CORS，支持移动应用
```
